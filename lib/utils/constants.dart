class AppConstants {
  static const String appName = 'YouTube Background';
  static const String apiKey = ''; // Здесь можно добавить ваш API ключ, если он нужен
  static const int maxResults = 20;
  static const Duration cacheDuration = Duration(days: 1);
  
  // Цвета
  static const Color primaryColor = Color(0xFFFF0000);
  static const Color backgroundColor = Color(0xFF000000);
  static const Color surfaceColor = Color(0xFF1A1A1A);
  static const Color textColor = Color(0xFFFFFFFF);
  static const Color secondaryTextColor = Color(0xFFAAAAAA);
}